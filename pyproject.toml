[project]
name = "fastapi-learning"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "uvicorn[standard]>=0.46.0",
]

[tool.fastapi]
entrypoint = "main:app"